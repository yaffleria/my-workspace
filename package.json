{
  "name": "my-workspace",
  "private": true,
  "version": "0.0.1",
  "packageManager": "yarn@4.10.3",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
